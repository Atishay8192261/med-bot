#############################
# External content hardening
#############################
NO_EXTERNAL=0

# DailyMed / openFDA call safety
DAILYMED_RATE_LIMIT_PER_MIN=15
OPENFDA_RATE_LIMIT_PER_MIN=60
EXTERNAL_REQUEST_TIMEOUT_SEC=20
EXTERNAL_BACKOFF_MAX_SEC=60

# Cache TTLs (days)
DAILYMED_TTL_DAYS=7
OPENFDA_TTL_DAYS=7

# openFDA key (optional)
OPENFDA_API_KEY=
OPENFDA_BASE=https://api.fda.gov/drug/label.json
DAILYMED_BASE=https://dailymed.nlm.nih.gov/dailymed/services/v2
# search backend switch
SEARCH_BACKEND=os  # or pg
# Database (Postgres)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=medbot
DB_USER=appuser
DB_PASS=apppass
# OpenSearch (dev only)
OS_URL=http://localhost:9200
OS_USER=admin
OS_PASS=admin
OS_INDEX_PREFIX=medbot
# API base (agent -> our FastAPI)
API_BASE_URL=http://localhost:8000
# openFDA (reserved for future)
OPENFDA_API_KEY=
# fallback tuning
EXTERNAL_MONO_TTL_DAYS=7
DAILYMED_RATE_LIMIT=15

# LLM fluency layer (optional)
LLM_ENABLED=0
LLM_PROVIDER=openai
OPENAI_API_KEY=
LLM_MODEL=gpt-4o-mini
LLM_TEMPERATURE=0.2
LLM_MAX_TOKENS=600
